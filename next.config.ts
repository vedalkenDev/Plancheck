import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Bin will host this under vedalken.dev/plancheck.
  // Leave basePath unset so `/` and `/plancheck` both work in this MVP repo.
};

export default nextConfig;
